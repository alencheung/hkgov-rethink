# Rust
/target
**/target
Cargo.lock.bak

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Local runtime artifacts
*.log
/data-local/
NDH6SA~M

# Local runtime persistence dir (v8 identity/insights/signals/investigations/
# feedback snapshots written by the file-based persist layer; configurable via
# HKGOV_PERSIST__DIR — local state, not source).
/data/

# Local agent tooling / session artifacts (not source of this project)
.agentic/
.zcode/
skills-staging/
agentic.json

# data dumps
.archive_all.json
*.archive.json
# press.html: a one-off generated data dump; not served (dashboard is embedded
# via include_str!) and not referenced anywhere in source.
press.html

# Scratch / probe working dir (live endpoint verification artifacts, not source)
.probe/

# Local QA/test harness artifacts (Phase 2/4 live probes + headless dashboard
# harness). These are verification scaffolding, not source — regenerated each
# QA cycle. Keep them out of the tree.
/phase2_*.py
/phase2_*.js
/phase2_results.json
/auth_debug.js
/auth_full_test.js
/auth_create_signal.js
/datasets_dom.js
/*_probe.json
/logs/
/package.json
/package-lock.json
/node_modules/

# Local Netlify folder
.netlify
