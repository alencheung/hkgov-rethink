{
  "name": "hkgov-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker fronting HK property portals (HKP, Midland, Chung Sen, AA Property) — geo-block bypass for the hkgov-rethink backend",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --remote",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
